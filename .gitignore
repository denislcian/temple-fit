# Dependencias
node_modules

# Builds
dist
dev-dist
coverage

# Logs y temporales
*.log
.DS_Store
*.local

# Editores
.vscode/*
!.vscode/extensions.json
.idea

# Originales de imagenes (5 MB c/u): solo se versionan las webp optimizadas
# (las instrucciones de la carpeta si entran en el repo)
src/img/*
!src/img/README.md

# Originales WAV de la musica (~35 MB c/u): solo se versionan los MP3
# comprimidos en public/music/. La barra inicial ancla la regla a la raiz para
# NO ignorar tambien public/music/ (donde viven los MP3 que usa la app).
/music/
